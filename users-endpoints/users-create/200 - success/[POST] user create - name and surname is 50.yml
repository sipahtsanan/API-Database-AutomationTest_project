info:
  name: "[POST] user create - name and surname is 50"
  type: http
  seq: 2

http:
  method: POST
  url: "{{user_endpoint}}/api/users"
  body:
    type: json
    data: |-
      {
          "cid": "{{gen_cid}}",
          "name": "runScript_name_valid_testttttttttttttttttttttttttt",
          "surname": "runScript_surename_valid_testttttttttttttttttttttt"
      }
  auth: inherit

runtime:
  scripts:
    - type: before-request
      code: |-
        // const cid = Math.floor(1000000000000 + Math.random() * 9000000000000).toString();
        // bru.setVar("gen_cid", cid);
    - type: tests
      code: |-
        const reqBody = req.getBody();
        const cid = reqBody.cid;
        const resBody = res.getBody();

        test("cid in response should match cid in request", function() {
          expect(resBody.data.cid).to.equal(cid);
        });
  assertions:
    - expression: res.body.message
      operator: contains
      value: User created successfully
    - expression: res.status
      operator: eq
      value: "201"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
