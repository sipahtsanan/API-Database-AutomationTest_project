info:
  name: "[GET] search user by cid not found"
  type: http
  seq: 4

http:
  method: POST
  url: "{{user_endpoint}}/api/users/search"
  body:
    type: json
    data: '{ "cid": "123456789012" }'
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        // const reqBody = req.getBody();
        // const cid = reqBody.cid;
        // const resBody = res.getBody();

        // test("message should contain cid", function() {
        //   expect(resBody.message).to.equal(`User with cid '${cid}' not found`);
        // });
  assertions:
    - expression: res.status
      operator: eq
      value: "400"
    - expression: res.body.message
      operator: eq
      value: cid must be exactly 13 digits

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
