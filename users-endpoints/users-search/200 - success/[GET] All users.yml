info:
  name: "[GET] All users"
  type: http
  seq: 1

http:
  method: POST
  url: "{{user_endpoint}}/api/users/search"
  body:
    type: json
    data: "{}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
